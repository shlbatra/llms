[project]
name = "ollama-example"
version = "0.1.0"
description = "Ollama local LLM examples"
authors = [{ name = "Sahil Batra" }]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "ollama>=0.4.0",
]

[project.optional-dependencies]
dev = [
    "ipykernel>=6.28.0",
]
